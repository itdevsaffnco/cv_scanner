services:
  web:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./SAFF_CV_Screener.html:/usr/share/nginx/html/index.html:ro
    restart: unless-stopped
